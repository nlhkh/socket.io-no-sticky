{
  "name": "socketionosticky",
  "version": "1.0.0",
  "description": "Use Socket.io 1.0 without sticky sessions when scaling out",
  "main": "app.coffee",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kha Nguyen",
  "license": "MIT",
  "dependencies": {
    "express": "^4.11.2",
    "redis": "^0.12.1",
    "socket.io": "^1.3.4",
    "socket.io-redis": "^0.1.4"
  }
}
